import React from "react";
import {
  ImageBackground,
  Dimensions,
  Image,
  StyleSheet,
  Text,
  View,
  ScrollView,
  TouchableOpacity,
} from "react-native";
const w = Dimensions.get("window").width;
const h = Dimensions.get("window").height;
export default function Favourite() {
  return (
    <View style={{ height: h / 2, width: w, backgroundColor: "white" }}>
      <Text>hhh</Text>
    </View>
  );
}
